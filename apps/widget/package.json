{
    "name": "@instantquote/widget",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "esbuild src/widget.ts --bundle --minify --format=iife --outfile=public/widget.js",
        "dev": "esbuild src/widget.ts --bundle --format=iife --outfile=public/widget.js --watch",
        "serve": "npx http-server ./public -p 8080"
    },
    "devDependencies": {
        "esbuild": "^0.19.12",
        "typescript": "^5.4.5"
    }
}

