# Copy this file to `apps/kaitlyn-nanny/.env.local` (do not commit secrets).
#
# Database (Required for production, optional for dev)
# Falls back to local JSON file if not set
DATABASE_URL=postgresql://user:password@host:5432/database

# Calendly Integration (Optional)
# Enable "Check open weekends" booking modal
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/your-username/your-event-type
CALENDLY_API_TOKEN=your_calendly_personal_access_token

# Email Notifications via Resend (Optional but recommended)
RESEND_API_KEY=re_xxxxxxxxxxxx
KAITLYN_INTAKE_TO=kaitlyn@example.com
KAITLYN_INTAKE_FROM=Kaitlyn Intake <intake@yourdomain.com>
KAITLYN_SEND_CONFIRMATION_EMAIL=true

# Admin Authentication (Recommended for production)
# Protects /admin routes with HTTP Basic Auth
ADMIN_BASIC_USER=admin
ADMIN_BASIC_PASS=your_secure_password

# Optional: Override fallback data directory
# Useful on Render if you want persistence via a disk mount
# KAITLYN_DATA_DIR=/path/to/persistent/storage

# Node Environment (auto-set by hosting platforms)
# NODE_ENV=production




